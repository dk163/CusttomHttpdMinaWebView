<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.kang.customhttpdmina"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.DUMP" />
    <uses-permission android:name="com.permission.MTKLOGGER" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_SUPERUSER" />

    <!-- BmobSDK权限 -->
    <!--获取GSM（2g）、WCDMA（联通3g）等网络状态的信息  -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!--for server need-->
    <!--<permission android:name="com.permission.MTKLOGGER" />-->
    <permission android:name="android.permission.DUMP" />


    <application
        android:name="com.kang.custom.application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_new"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.kang.custom.activity.MainActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.kang.custom.activity.WebViewActivity">
            <!--            <intent-filter>
                            <action android:name="android.intent.action.MAIN" />
                            <category android:name="android.intent.category.LAUNCHER" />
                        </intent-filter>-->
        </activity>

        <service
            android:name="com.kang.custom.service.MinaServer"
            android:exported="true" />
        <service
            android:name="com.kang.custom.service.MinaClient"
            android:exported="true" />

        <receiver
            android:name="com.kang.custom.receiver.HttpdReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="httpd_start_msg" />
                <action android:name="httpd_stop_msg" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.kang.custom.receiver.MtkReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.mediatek.mtklogger.intent.action.LOG_STATE_CHANGED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
